export type FilterValues = {
    nameSearch: string;
    manufacturer: string | null;
    type: string | null;
    pushFeel: [number, number];
    wobble: [number, number];
    sound: [number, number];
    context: [number, number];
    other: [number, number];
    total: [number, number];
};
